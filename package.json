{
  "name": "chrome-web-agent",
  "version": "1.0.0",
  "description": "这是一个可运行的 Chrome 扩展： 通过自然语言 + function calling 实际操作当前网页（读、改、抓取、整页翻译），并支持接入 MCP 网关工具。",
  "main": "background.bundle.js",
  "scripts": {
    "build": "esbuild background.js --bundle --format=esm --target=chrome114 --outfile=background.bundle.js",
    "build:watch": "esbuild background.js --bundle --format=esm --target=chrome114 --outfile=background.bundle.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "esbuild": "^0.27.3",
    "openai": "^6.22.0"
  }
}
